# dotfiles
Blake dotfiles

```
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply trodemaster
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply trodemaster --use-builtin-git true
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --promptString environment=personal --apply trodemaster


```
